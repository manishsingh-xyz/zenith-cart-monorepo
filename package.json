{
  "name": "zenith-cart",
  "version": "1.0.0",
  "private": true,
  "description": "Monorepo for Zenith Cart micro frontend ecommerce app",
  "scripts": {
    "dev:host": "pnpm --filter host dev",
    "dev:products": "pnpm --filter products dev",
    "dev:cart": "pnpm --filter cart dev",
    "build:host": "pnpm --filter host build",
    "build:products": "pnpm --filter products build"
  },
  "workspaces": [
    "packages/*",
    "host",
    "products"
  ],
  "license": "MIT"
}
